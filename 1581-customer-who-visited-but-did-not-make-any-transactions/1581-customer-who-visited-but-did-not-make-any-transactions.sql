/* Write your T-SQL query statement below */
SELECT V.customer_id, count(V.visit_id) as count_no_trans  FROM Visits AS V LEFT JOIN Transactions AS T ON V.visit_id = T.visit_id WHERE transaction_id IS NULL GROUP BY V.customer_id;